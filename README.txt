1.) Use CMake to generate the build folder.
2.) Take the picture in /res that's called loading.jpg and place it in the build folder. Not the debug folder, but the build folder itself.
3.) Add the necessary .dlls to the folder where the executable is inside the build folder (debug).